services:
  commet:
    image: localhost/commet:latest
    container_name: commet
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: 10m
        max-file: 3
    networks:
      - commet-workspace-network
networks:
  commet-workspace-network:
    external: true
