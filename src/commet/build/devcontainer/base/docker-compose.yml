services:
  commet:
    image: commet
    build:
      context: .
      args:
        RELEASE: ${RELEASE}
    container_name: commet
    restart: unless-stopped
    networks:
      - commet-workspace-network
networks:
  commet-workspace-network:
    external: true
