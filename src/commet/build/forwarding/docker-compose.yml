services:
  commet:
    image: localhost/commet:latest
    container_name: commet
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: 10m
        max-file: 3
    ports:
      - "3000:80"
